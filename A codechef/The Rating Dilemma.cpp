#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main(){
	int t;
	cin >> t;
	while(t--){
		ll a, b, c, d;
		ll mx = max(max(a, b), max(c, d));
		ll(int i = 1; i < mx; i++){
			if(mx == 	)
		}
	}
	return 0;
}



